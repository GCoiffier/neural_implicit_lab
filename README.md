
# Conventions

- All input geometries are centered around zero and scaled down so that they fit inside `[-1, 1]^n`
- By default, datasets of points are sampler inside the `[-1.5, 1.5]^n` domain 


# TODO
rely on `meshio` for faster loading